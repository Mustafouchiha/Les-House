# Base URL of the backend API. In dev, leave empty to use the Vite proxy (/api).
# In production set it to the deployed backend origin, e.g. https://api.taxtabozor.uz
VITE_API_URL=

# Dev-only: target the Vite proxy forwards /api to.
VITE_API_PROXY=http://localhost:8080
